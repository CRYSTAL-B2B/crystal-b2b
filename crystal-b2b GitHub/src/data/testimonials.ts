/**
 * Отзывы клиентов.
 *
 * Массив намеренно пуст. Все цифры на странице звучат от первого лица, и
 * отзыв - единственное место, где говорит третья сторона; выдуманный отзыв
 * обесценил бы и настоящие кейсы рядом. Пока владелец не прислал согласованные
 * с клиентами тексты, секция не выводится вовсе - см. Testimonials.tsx.
 *
 * Чтобы опубликовать: добавить записи сюда. Нужны текст (2-3 предложения),
 * имя, должность, компания и подтверждение, что публикация согласована с
 * клиентом. Вёрстка и стили уже готовы, менять их не потребуется.
 *
 * Тире в текстах - обычный дефис: короткие и длинные тире на сайте не
 * используются, это проверяет e2e-тест.
 */

export type Testimonial = {
  id: string;
  text: string;
  /** Имя. */
  author: string;
  /** Должность. */
  role: string;
  company: string;
};

export const testimonials: readonly Testimonial[] = [];

export const testimonialsCopy = {
  title: "Что говорят те, с кем работали.",
} as const;
